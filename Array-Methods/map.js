//This method is used to map each array item over to another array's item,
// Returns array of the same length


let colors = ['apple','banana','kiwi']

colors.map((color,index)=>{
    console.log(`${index} : ${color}`)
})

